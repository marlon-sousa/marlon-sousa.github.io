// Place any global data in this file.
// You can import this data from anywhere in your site by using the `import` keyword.
//
// Only what reads the same in every language lives here. Anything that has to be
// said differently in Portuguese — the site description, the home page title —
// is a key in src/i18n/ui.ts instead.

export const SITE_TITLE = 'Marlon Brandão de Sousa';
export const SITE_AUTHOR = 'Marlon Brandão de Sousa';
// The published contact address. It lives here, and not written out in the two
// places that show it, so moving to a forwarding alias later is one edit.
export const SITE_EMAIL = 'contact@marlon-sousa.com';
// The domain, as it is shown to a reader rather than as it is linked. The Open
// Graph cards print it, and it survived the move to marlon-sousa.com written out
// by hand in the card renderer, where nobody thought to look. It lives here now
// so the next move is one edit.
export const SITE_DOMAIN = 'marlon-sousa.com';
